# Copy this file to .env and fill in your key to enable AI-assisted extraction.
# Without this, the pipeline automatically falls back to a rule-based extractor.
ANTHROPIC_API_KEY=

# Optional: override the default model
ANTHROPIC_MODEL=claude-sonnet-5
